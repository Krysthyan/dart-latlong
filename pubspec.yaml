name: latlong
description: Lightweight library for common latitude and longitude calculation
author: Mike Mitterer <office@mikemitterer.at>
homepage: https://github.com/MikeMitterer/dart-latlong

version: 0.6.1

environment:
  sdk: ">=2.0.0 <3.0.0"

dependencies:
  logging: ^1.0.1
  console_log_handler:
    git:
      url: https://github.com/AntonHomeniuk/dart-console_log_handler.git

  validate: ^1.7.0

  intl: ^0.17.0

dev_dependencies:
  test: any

  build_runner: any
  build_test: any
  build_web_compilers: any
